# ── Core PDF parsing ──────────────────────────────────────────────────────────
# Primary: LlamaParse (AI-powered, handles OCR + tables natively)
llama-parse>=0.5.0
llama-index-core>=0.11.0

# Secondary: PyMuPDF (fast local parser, table detection, page rendering)
pymupdf>=1.24.0

# Fallback: pdfplumber + pypdf (always available, text-only)
pdfplumber>=0.11.0
pypdf>=4.0.0

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract>=0.3.10      # local Tesseract binding (fallback OCR)
# System dep: sudo apt install tesseract-ocr tesseract-ocr-eng

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0
pdf2image>=1.17.0        # poppler-based fallback renderer
# System dep: sudo apt install poppler-utils

# ── LLM providers ─────────────────────────────────────────────────────────────
# AWS Bedrock (recommended for production — IAM auth, no API keys)
boto3>=1.34.0

# Anthropic direct (local dev)
anthropic>=0.40.0
httpx>=0.27.0

# Google Gemini
google-generativeai>=0.8.0

# OpenAI
openai>=1.50.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
